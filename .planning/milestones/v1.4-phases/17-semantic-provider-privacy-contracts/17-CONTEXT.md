# Phase 17 Context

Goal: define local-only semantic provider contracts, privacy status, safe inventory gating, and source-free storage behavior without changing ctxhelm's read-only boundary.

Implementation evidence: `SemanticProviderConfig`, `SemanticOptions`, disabled-by-default diagnostics, schema v2 semantic vector metadata, and source-free storage tests.

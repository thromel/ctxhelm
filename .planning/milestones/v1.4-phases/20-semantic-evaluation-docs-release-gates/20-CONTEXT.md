# Phase 20 Context

Goal: prove semantic retrieval through source-free eval metadata, docs, and release-gate smoke while preserving cloud-disabled defaults.

Implementation evidence: semantic-enabled historical eval filters/range IDs, benchmark config support, `docs/semantic.md`, release docs, and `scripts/smoke-semantic.sh`.

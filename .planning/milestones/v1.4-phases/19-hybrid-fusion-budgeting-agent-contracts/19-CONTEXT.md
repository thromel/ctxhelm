# Phase 19 Context

Goal: fuse semantic candidates with the existing context compiler while preserving exact-match priority, compact output, and additive agent contracts.

Implementation evidence: `RetrievalSignalKind::Semantic`, `RankingInput.semantic_results`, secondary semantic weight, semantic provenance in candidates, and compatibility tests.

# Phase 18 Context

Goal: generate local vector candidates from safe files, persist/reuse vector metadata, and expose semantic candidates through existing CLI and MCP workflows.

Implementation evidence: `semantic_search_report`, `semantic_vector_records`, incremental semantic storage reuse counts, CLI `--semantic`, and MCP `semantic` arguments.

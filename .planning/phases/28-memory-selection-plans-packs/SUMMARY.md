# Phase 28 Summary

`prepare-task` now selects bounded memory candidates with evidence. `get-pack`
renders selected memory separately from target files and test snippets. MCP
clients can read memory through `ctxpack://repo/memory`.
